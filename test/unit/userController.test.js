const expect = require('chai').expect
const assert = require('assert');
const { addUser, getAllUsers } = require('../../prisma/controllers/users.js')

describe('User', async () => {
  it ('should get all the users', () => {

  })
})